import { githubConfig, siteConfig } from "@api-config";

export const SITE_OWNER = siteConfig.owner;
export const CONTACT_EMAIL = siteConfig.contactEmail;
export const GITHUB_USER = githubConfig.user;
export const GITHUB_URL = githubConfig.url;
export const FEATURED_WORK = siteConfig.featuredWork;
